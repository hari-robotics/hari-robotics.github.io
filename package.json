{
  "name": "typescript-resume-template",
  "version": "1.0.0",
  "private": true,
  "description": "A static resume template generated by TypeScript",
  "scripts": {
    "build:app": "tsc -p tsconfig.json",
    "build:pdf": "node --experimental-websocket scripts/export-pdf.mjs",
    "build": "npm run build:app && npm run build:pdf",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "export:pdf": "npm run build:app && npm run build:pdf"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
